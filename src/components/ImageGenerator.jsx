import React, { useState } from 'react'; function ImageGenerator() { const [prompt, setPrompt] = useState(''); const [imageUrl, setImageUrl] = useState(''); const generateImage = async () => { if (!prompt) return; try { const res = await fetch('https://api.openai.com/v1/images/generations', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${import.meta.env.VITE_OPENAI_API_KEY}` }, body: JSON.stringify({ prompt: prompt, n: 1, size: "512x512" }) }); const data = await res.json(); setImageUrl(data.data[0].url); } catch (err) { console.error(err); } }; return (<div className="bg-white/10 p-6 rounded-xl shadow-xl backdrop-blur-md space-y-4"><h2 className="text-2xl font-bold">AI Image Generator</h2><input value={prompt} onChange={(e)=>setPrompt(e.target.value)} placeholder="Deskripsikan gambar..." className="w-full p-2 rounded-md bg-white/20 placeholder-white/70 text-white outline-none"/><button onClick={generateImage} className="bg-gradient-to-r from-pink-500 to-yellow-400 px-4 py-2 rounded-md font-bold">Generate</button>{imageUrl && <img src={imageUrl} alt="AI Generated" className="w-full rounded-md mt-4 border border-white/20" />}</div>); } export default ImageGenerator;